const TripCreationLayout = ({ children }: { children: React.ReactNode }) => {
  return (
    <div className="w-full h-full grid place-content-center">{children}</div>
  );
};

export default TripCreationLayout;
